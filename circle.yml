database:
  pre:
    - pwd
    - ls -la
  post:
    - ls -la
  override:
  - pwd
  - psql -U ubuntu circle_test < ./create.sql
  
test:
  pre:
    - pwd
    - ls -la
    - cd ./demo
    - pwd
    - ls -la
  override:
    - mocha